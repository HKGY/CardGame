window.CG = window.CG || {};

/* ===========================================================================
 *  背景音乐 —— 已移除（原先依赖 mp3 文件）。
 * ===========================================================================
 *  按需求「去掉 mp3、全部改用朴素实现」：此处保留同名空接口，避免改动各调用点；
 *  战斗 / 界面音效仍由 audio.js 用 Web Audio 即时合成（无任何音频文件）。
 *  若日后想要纯合成的氛围音乐，在这里实现 playScene 即可。
 * ===========================================================================
 */
(function (CG) {
  const noop = () => {};
  CG.Music = { init: noop, playScene: noop, start: noop, setMuted: noop };
})(window.CG);
